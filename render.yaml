services:
  - type: web
    name: grime-guys-backend
    runtime: node
    plan: free
    buildCommand: |
      cd backend
      npm install
      npm rebuild sqlite3
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
